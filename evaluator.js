function getPrecision(likedItems, recommendedItems) {
    //TODO actually calculate
    return 1.0;
}

function getNDCG(likedItems, recommendedItems) {
    //TODO actually calculate
    return 1.0;
}
